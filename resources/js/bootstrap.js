/**
 * This file loads the dependencies for the application.
 */

try {
    // If using Axios for API calls (Records/Enrollment)
    // window.axios = require('axios');
    // window.axios.defaults.headers.common['X-Requested-With'] = 'XMLHttpRequest';
    
    console.log('University of Mindanao Assets Loaded Successfully.');
} catch (e) {
    console.error('Asset load error:', e);
}
